
# Система описания и исполнения клеточных автоматов

Данный проект представляет собой программную систему для моделирования и визуализации различных типов клеточных автоматов. Работа выполнена в рамках курсового проекта по дисциплине **«Параллельное программирование»** (Задача №9).

## 📋 Описание проекта

Система позволяет описывать правила клеточных автоматов и исполнять их на двумерной сетке с использованием параллельных вычислений. В основе лежит принцип разделения логики (Engine), данных (Grid) и правил (Rules), что обеспечивает высокую расширяемость.

### Основные возможности:
* **Параллельные вычисления:** Расчет нового состояния поля распределяется между ядрами процессора с использованием Kotlin Coroutines.
* **Гибкая система правил:** Поддержка различных типов автоматов (Conway's Game of Life, Seeds, Day & Night) и возможность работы с вещественными числами (Average Rule).
* **Тороидальная топология:** Поле замкнуто само на себя (границы закольцованы), что позволяет объектам беспрепятственно пересекать края экрана.
* **Системный аудит:** Мониторинг производительности в реальном времени (время шага, количество задействованных ядер, статистика живых клеток).
* **Библиотека паттернов:** Быстрая инициализация поля классическими фигурами (Планер, Осциллятор, Квадрат).

## 🛠 Технологический стек

* **Язык:** [Kotlin](https://kotlinlang.org/) (1.9+)
* **Параллелизм:** [Kotlin Coroutines](https://kotlinlang.org/docs/coroutines-overview.html)
* **Интерфейс:** Java Swing
* **Сборка:** Gradle
* **Тестирование:** JUnit 5, Coroutines Test

## 🏗 Архитектура системы

Проект спроектирован с соблюдением принципов декомпозиции:
1.  **`Grid<T>`**: Универсальная матрица данных с поддержкой тороидальной адресации.
2.  **`Engine`**: Вычислительное ядро. Разбивает сетку на сегменты (chunks) и обрабатывает их параллельно.
3.  **`AutomationRule<T>`**: Интерфейс для описания логики перехода состояний. 
4.  **`CellularAutomataUI`**: Графическая оболочка для управления параметрами симуляции.



## 🚀 Запуск проекта

1. Убедитесь, что у вас установлена JDK 17 или выше.
2. Клонируйте репозиторий:
   ```bash
   git clone [https://github.com/alfoll/Cellular-Automata-KRA.git]
3. **Запуск в среде разработки:**
    * Откройте проект в IntelliJ IDEA как **Gradle-проект**.
    * Запустите основной класс: `org.example.kra_rosl_9.UI.CellularAutomataUI`.

4. **Управление:**
    * **Размер:** Введите число от 10 до 500 и нажмите кнопку **"Применить размер и сброс"**.
    * **Начальные данные:** Выберите пресет (например, *"Планер"*) перед стартом.
    * **Старт/Стоп:** Кнопки управления жизненным циклом симуляции.

---

### 🧪 Тестирование
Для обеспечения надежности реализован пакет функциональных тестов в папке `src/test`:

* **Модульные тесты:** Проверка математической корректности каждого правила.
* **Тесты топологии:** Валидация метода `getIndex` для работы с отрицательными координатами.
* **Интеграционный тест:** Проверка стабильности `Engine` при параллельной обработке поколений.

**Запуск всех тестов:**
```bash
./gradlew test
```

### 📈 Системный аудит
Программа ведет непрерывную трассировку выполнения. В правой панели UI выводится:

* **Поколение:** Номер текущего шага.
* **Время:** Скорость обработки всей сетки в мс.
* **Ресурсы:** Количество реально задействованных логических ядер CPU.
* **Популяция:** Текущее количество живых и мертвых клеток.

### Автор - Поминова Александра
